{
  "name": "homebridge-roof-control",
  "version": "1.0.0",
  "description": "",
  "main": "dist/plugin.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "rimraf ./dist && tsc",
    "prepublishOnly": "npm run build",
    "postpublish": "npm run clean",
    "start": "ts-node-dev --respawn --transpile-only src/dev.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Byrne",
  "engines": {
    "homebridge": ">=1.0.0"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "license": "MIT",
  "dependencies": {
    "pi-spi": "^1.2.1",
    "rpi-gpio": "^2.1.7",
    "rpio": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.10",
    "@types/pi-spi": "^0.0.28",
    "@types/rpio": "^2.4.2",
    "homebridge": "^1.4.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5",
    "ts-node-dev": "^1.1.8"
  }
}
